const eventName = "Community Cleanup";
const eventDate = "2025-06-15";
let availableSeats = 30;

console.log(`${eventName} is on ${eventDate} and has ${availableSeats} seats available.`);

// Simulate a user registering
availableSeats--;
console.log(`Seats left after registration: ${availableSeats}`);
